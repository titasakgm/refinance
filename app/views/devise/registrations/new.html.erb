<%= devise_error_messages! %>

<div class="row buffer-top">
  <div class="col-sm-offset-3 col-sm-6">
    <div class="panel panel-info">
      <div class="panel-heading center">ลงทะเบียนเพื่อทำแบบประเมินสุขภาพทางการเงิน</div>
      <div class="panel-body">
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <div class="form-group">
            <%= f.input :email, as: :string, required: true, autofocus: true, :label => "Username",
            :placeholder => "pid13@hcode5", :input_html => { "data-tooltip" => true,
            :title => "ใส่เลขประชาชน 13 หลัก ตามด้วย @ และรหัสหน่วยงาน 5 หลัก เช่น 1234567890123@12345" } %>
          </div>

          <div class="form-group">
            <%= f.input :telno, autofocus: true, :label => "Mobile",
            :placeholder => "เบอร์โทรศัพท์บ้าน หรือเบอร์มือถือ" %>
          </div>

          <div class="form-group">
            <%= f.input :password, required: true, :label => "รหัสผ่าน", :input_html => { "data-tooltip" => true,
            :title => "รหัสผ่านต้องมีความยาวอย่างน้อย 6 ตัวอักษร", :placeholder => "อย่างน้อย 6 ตัวอักษร" } %>
          </div>

          <div class="form-group">
            <%= f.input :password_confirmation, required: true, :label => "ยืนยัน" %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, "ลงทะเบียน", :class => "btn btn-success" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <div class="col-sm-offset-3 col-sm-6 center">
      <span>
        <%= link_to "เข้าสู่ระบบ", surveyor_path, :class => "btn btn-success text-white" %>

        <%= link_to "ค้นหารหัสหน่วยงาน 5 หลัก", "http://203.157.10.8/hcode_2014/query_set.php?p=3", { :class => "btn btn-warning search", :target => "_blank" } %>
      </span>
  </div>

  <div id="step" class="col-md-6 col-md-offset-3">
    <span>ขั้นตอนการลงทะเบียน</span>
    <ol style="list-style: decimal inside;">
      <li>กดปุ่ม [ลงทะเบียน] ในหน้าแรก</li>
      <li>ค้นหาเลขรหัสหน่วยงาน 5 หลัก โดยกดปุ่ม [ค้นหาเลขรหัสหน่วยงาน 5 หลัก]</li>
      <li>ในหน้าค้นหารหัสหน่วยงาน โดยเลือกหน่วยงานต้นสังกัดระดับกรม หน่วยปฏิบัติงาน
        และจังหวัด ตามลำดับ แล้วกดปุ่ม [ค้นหาข้อมูล]</li>
      <li>ตรวจสอบเลขรหัส 5 หลัก ของหน่วยงานที่ท่านปฏิบัติงานอยู่</li>
      <li>ในหน้า "ลงทะเบียนเพื่อทำแบบประเมินฯ" ให้กำหนด Username เพื่อ "เข้าสู่ระบบ"
        ประกอบด้วย "เลขบัตรประชาชน 13 หลัก" ตามด้วย "@" และตามด้วย "เลขรหัสหน่วยงาน 5 หลัก" เช่น 123456789012@12345</li>
      <li>ใส่หมายเลขโทรศัพท์ ในช่อง "หมายเลขโทรศัพท์"</li>
      <li>กำหนดรหัสผ่าน ด้วยตัวอักษร หรือตัวเลข จำนวน 6 หลัก และยืนยันรหัสผ่าน ในช่อง
        "ยืนยัน" ด้วยรหัสเดิม</li>
      <li>กดปุ่ม [ลงทะเบียน] แล้ว กดปุ่ม [เข้าสู่ระบบ]</li>
      <li>หลัง "เข้าสู่ระบบ" แล้ว ให้กดปุ่ม [เริ่มต้น] เพื่อทำแบบประเมิน</li>
    </ol>

    <div id="note">
      หมายเหตุ หากเข้าสู่ระบบแล้ว ต้องการกลับไปแก้ไขข้อมูลให้กด 2 ครั้ง ที่บริเวณเลข 13 หลัก
      (แถบสีเขียว) ตรงมุมบนขวาของหน้าจอ
    </div>

  </div>

</div>

